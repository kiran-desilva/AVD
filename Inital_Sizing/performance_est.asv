clear 
clc

load("sizing.mat");
load("parameters.mat");

%% Takeoff 

Vinit = 0;
Vstall_clean = ;
Vstall_TO = sqrt((2 * sizing.W0) / (1.225 * ;
V_TO = 1.1 * Vstall_TO; %FAR-25

perf.Ka = (rho / (2 * W / Sref)) * (mu * Cl - Cd0 - (Cl^2) / (pi * AR *e));
perf.Kt = T / W - mu;
perf.Sg = (1 / (2 * g * perf.Ka)) * log(abs((perf.Kt + perf.Ka * Vtakeoff^2) / (perf.Kt + perf.Ka * Vinit^2));

%%
